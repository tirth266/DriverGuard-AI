# Server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
SQLAlchemy==2.0.36

# JWT
python-jose[cryptography]==3.3.0

# Google OAuth (Authlib with Starlette integration)
Authlib==1.4.1
requests==2.32.3
httpx==0.27.2

# Session middleware (for OAuth state/CSRF)
itsdangerous==2.2.0

# Environment & Config
python-dotenv==1.1.0

# CORS — bundled with FastAPI/Starlette, no extra package needed

# Password hashing
bcrypt==4.3.0

# Computer Vision & AI
opencv-python==4.10.0.84
numpy==2.1.3
mediapipe>=0.10.30
Pillow==10.4.0
ultralytics>=8.3.0
torch>=2.0.0

# Form data / file upload support for FastAPI
python-multipart==0.0.12